# 系统设置移植 - 多模态转述控制 (astrbot_plugin_sys_setting_port)

这是一个为 AstrBot 打造的精细化图片转述控制插件。

## 🌟 核心功能

AstrBot 系统自带的「默认图片转述模型」是全局生效的，一旦开启，所有模型在遇到图片时都会调用转述。
本插件旨在解决这一痛点，提供以下高级特性：

1. **按需转述**：只有当当前对话使用的是您在 `target_models` 列表中指定的纯文本模型时，才会触发图片转述。
2. **优雅注入**：转述结果会作为 `[系统附加信息 - 图片转述]` 注入到 `system prompt` 中，保持用户对话历史的纯净。
3. **智能重试**：当多模态模型“抽风”返回空值，或者回复包含“抱歉”、“无法识别”等关键词时，插件会自动进行重试。
4. **终极兜底**：如果主转述模型重试耗尽依然失败，插件会自动无缝切换到您配置的次选兜底模型。
5. **精准触发**：插件仅在 AI 被唤醒（如被 @、触发唤醒词或私聊）时才会进行转述，绝不会无脑读取群聊中的闲聊图片，为您节省大量 Token。
6. **自定义等待机制 (增强版)**：接管系统原有的「只 @ 机器人」等待逻辑，支持自定义等待时间、自定义等待回复词。
7. **严格用户隔离**：在群聊中，只有真正 @ 机器人的那个用户发送的下一条消息才会被捕获，完美避免其他群友的闲聊乱入！
8. **完美超时唤醒**：超时后若用户未发消息，强制唤醒 AI 主动找话题，并且若有其余插件引用功能， AI 回复会**正确引用**用户之前发送的那条 @ 消息，体验丝滑原生！
9. **私聊主动回复 (AGI 进化版)**：当私聊闲置时，机器人会主动找用户聊天。**核心优势**：
   - **纯净上下文架构**：直接调用 LLM 并手动触发钩子，完美触发所有下游插件（如语音、表情包等），不会将冗长的系统提示词污染到数据库的历史记录中！
   - **双模式驱动**：支持“纯随机模式”和“智能判定模式”。智能模式下，Bot 会调用轻量级模型“察言观色”，根据聊天氛围决定是否主动开口。
   - **极致人文关怀**：支持免打扰时段（半停机不耗资源）、白名单机制、未回复次数上限，以及动态渲染的高级提示词！
   - **硬盘级持久化**：主动回复的状态（闲置时间、未回复次数等）会安全地保存在 `data/plugin_data` 目录下，即使重启 AstrBot 或重载插件，时间线也绝对不会丢失！

## ⚙️ 配置说明

请在 AstrBot 的 WebUI 后台 -> 插件管理 -> `astrbot_plugin_sys_setting_port` 中进行配置：

### ⏳ 等待机制配置
- **启用自定义等待机制**：开启后接管系统的等待逻辑。
- **自定义等待时间 (秒)**：只 @ 机器人后，等待用户发送下一条消息的超时时间。
- **等待期间是否回复** / **自定义等待回复词**：等待期间机器人发送的提示文本。
- **超时后是否强制唤醒**：超时后若用户未发消息，直接唤醒 AI 主动回复。

### 💬 主动回复配置
- **允许主动回复的用户列表**：白名单机制，只有填入此列表的 QQ 号/用户 ID 才会获得主动消息服务。
- **主动回复触发模式**：选择“纯随机模式”或“智能判定模式”。
- **智能判定模型 / 提示词**：仅智能模式有效，用于判定当前氛围是否适合主动聊天。
- **最小 / 最大触发间隔 (分钟)**：控制主动消息的频率和保底触发时间。
- **免打扰时段 (24小时制)**：例如 `23-7`，在此期间插件进入半停机状态，不发送消息也不调用判定模型。
- **最大未回复次数上限**：防止无限骚扰，达到上限后暂停主动回复。
- **主动回复触发提示词**：支持 `{{current_time}}` 和 `{{unanswered_count}}` 占位符，指导大模型根据情境动态调整语气。

### 🖼️ 多模态转述配置
- **多模态转述模型**：选择用于看图的主力模型（如 `openai`, `gemini` 等）。
- **兜底次选转述模型**：主模型失败时的备用模型。
- **需要转述的模型列表**：填入需要被转述的纯文本模型名称（如 `gpt-3.5-turbo`, `deepseek-v3`）。**支持模糊匹配**，只要当前模型名称包含列表中的关键词（不区分大小写）即可触发转述。
- **图片转述提示词**：自定义发送给多模态模型的看图指令。
- **最大重试次数** / **触发重试的关键词**：控制重试逻辑。

## ⚠️ 注意事项

1. **关闭系统转述**：使用本插件前，请务必将 AstrBot 系统设置（LLM 配置）中的「默认图片转述模型」留空！
2. **关闭系统等待**：如果您启用了本插件的「自定义等待机制」，请务必在系统设置（平台设置）中关闭原有的「只 @ 机器人是否触发等待」！

## 📝 更新日志

### v1.8.0
- **新增**：主动回复状态的硬盘级持久化。重启或重载插件不再丢失闲置时间和未回复次数。
- **修复**：彻底解决插件重载时 `terminate` 方法导致的异步报错问题。

### v1.7.0
- **重构**：主动回复采用“纯净上下文架构”，直接调用 LLM 并手动触发钩子，彻底解决长提示词污染数据库历史记录的问题。

### v1.6.0
- **新增**：多模态转述模型列表支持**模糊匹配**（不区分大小写）。
- **新增**：主动回复支持“智能判定模式”，可调用轻量级模型判断当前氛围是否适合主动聊天。
- **新增**：主动回复支持免打扰时段（半停机机制），极致节省资源。

### v1.5.0
- **新增**：私聊主动回复功能（走正门版）。支持白名单机制、未回复次数上限，以及动态渲染的高级提示词。

### v1.4.0
- **修复**：修复了超时唤醒逻辑中，伪造事件被底层系统拦截丢弃的问题。

### v1.3.0
- **新增**：严格用户隔离。群聊中只有真正 @ 机器人的用户才能触发后续等待逻辑。
- **新增**：完美超时唤醒。超时后 AI 的回复会完美引用用户之前发送的那条 @ 消息。